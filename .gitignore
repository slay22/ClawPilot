### DotnetCore ###
# .NET Core build folders
bin/
obj/

.idea/
.DS_Store

# Common node modules locations
/node_modules
/wwwroot/node_modules
Arte.Apax.Pa.Backend.sln.DotSettings.user

# === SECRETS — never commit these ===
appsettings.Local.json
appsettings.*.Local.json
.env
!.env.example

# .NET user secrets (stored outside repo by dotnet, but just in case)
secrets.json

data/
logs/
